/* 1-(1/2)+(1/3)-(1/4)+...+(1/99)-(1/100)*/
#include <stdio.h>
int main()
{
  int sign = 1;
  double sum = 1.0;
  double term;
  while(deno <= 100)
  {
  sign = -sign;
  term = sign/deno;
  sum += term;
  ++deno;
  }
  printf("sum id %lf\n", sum);
  return 0;
}
